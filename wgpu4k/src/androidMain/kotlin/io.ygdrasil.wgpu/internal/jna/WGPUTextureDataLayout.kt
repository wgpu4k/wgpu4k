// Generated by jextract
package io.ygdrasil.wgpu.internal.jna

import java.lang.foreign.AddressLayout
import java.lang.foreign.Arena
import java.lang.foreign.GroupLayout
import java.lang.foreign.MemoryLayout.Companion.sequenceLayout
import java.lang.foreign.MemoryLayout.Companion.structLayout
import java.lang.foreign.MemoryLayout.PathElement.groupElement
import java.lang.foreign.MemorySegment
import java.lang.foreign.SegmentAllocator
import java.lang.foreign.ValueLayout
import java.util.function.Consumer

/**
 * {@snippet lang=c :
 * * struct WGPUTextureDataLayout {
 * *     const WGPUChainedStruct *nextInChain;
 * *     uint64_t offset;
 * *     uint32_t bytesPerRow;
 * *     uint32_t rowsPerImage;
 * * }
 * * }
 */
object WGPUTextureDataLayout {
    private val `$LAYOUT` = structLayout(
        wgpu_h.C_POINTER.withName("nextInChain"),
        wgpu_h.C_LONG_LONG.withName("offset"),
        wgpu_h.C_INT.withName("bytesPerRow"),
        wgpu_h.C_INT.withName("rowsPerImage")
    ).withName("WGPUTextureDataLayout")

    /**
     * The layout of this struct
     */
    @JvmStatic
    fun layout(): GroupLayout {
        return `$LAYOUT`
    }

    private val `nextInChain$LAYOUT`: AddressLayout = `$LAYOUT`.select(groupElement("nextInChain")) as AddressLayout

    /**
     * Layout for field:
     * {@snippet lang=c :
     * * const WGPUChainedStruct *nextInChain
     * * }
     */
    fun `nextInChain$layout`(): AddressLayout {
        return `nextInChain$LAYOUT`
    }

    private const val `nextInChain$OFFSET`: Long = 0

    /**
     * Offset for field:
     * {@snippet lang=c :
     * * const WGPUChainedStruct *nextInChain
     * * }
     */
    fun `nextInChain$offset`(): Long {
        return `nextInChain$OFFSET`
    }

    /**
     * Getter for field:
     * {@snippet lang=c :
     * * const WGPUChainedStruct *nextInChain
     * * }
     */
    fun nextInChain(struct: MemorySegment): MemorySegment {
        return struct.get(`nextInChain$LAYOUT`, `nextInChain$OFFSET`)
    }

    /**
     * Setter for field:
     * {@snippet lang=c :
     * * const WGPUChainedStruct *nextInChain
     * * }
     */
    fun nextInChain(struct: MemorySegment, fieldValue: MemorySegment?) {
        struct.set(
            `nextInChain$LAYOUT`, `nextInChain$OFFSET`,
            fieldValue!!
        )
    }

    private val `offset$LAYOUT` = `$LAYOUT`.select(groupElement("offset")) as ValueLayout.OfLong

    /**
     * Layout for field:
     * {@snippet lang=c :
     * * uint64_t offset
     * * }
     */
    fun `offset$layout`(): ValueLayout.OfLong {
        return `offset$LAYOUT`
    }

    private const val `offset$OFFSET`: Long = 8

    /**
     * Offset for field:
     * {@snippet lang=c :
     * * uint64_t offset
     * * }
     */
    fun `offset$offset`(): Long {
        return `offset$OFFSET`
    }

    /**
     * Getter for field:
     * {@snippet lang=c :
     * * uint64_t offset
     * * }
     */
    fun offset(struct: MemorySegment): Long {
        return struct.get(`offset$LAYOUT`, `offset$OFFSET`)
    }

    /**
     * Setter for field:
     * {@snippet lang=c :
     * * uint64_t offset
     * * }
     */
    fun offset(struct: MemorySegment, fieldValue: Long) {
        struct.set(`offset$LAYOUT`, `offset$OFFSET`, fieldValue)
    }

    private val `bytesPerRow$LAYOUT` = `$LAYOUT`.select(groupElement("bytesPerRow")) as ValueLayout.OfInt

    /**
     * Layout for field:
     * {@snippet lang=c :
     * * uint32_t bytesPerRow
     * * }
     */
    fun `bytesPerRow$layout`(): ValueLayout.OfInt {
        return `bytesPerRow$LAYOUT`
    }

    private const val `bytesPerRow$OFFSET`: Long = 16

    /**
     * Offset for field:
     * {@snippet lang=c :
     * * uint32_t bytesPerRow
     * * }
     */
    fun `bytesPerRow$offset`(): Long {
        return `bytesPerRow$OFFSET`
    }

    /**
     * Getter for field:
     * {@snippet lang=c :
     * * uint32_t bytesPerRow
     * * }
     */
    fun bytesPerRow(struct: MemorySegment): Int {
        return struct.get(`bytesPerRow$LAYOUT`, `bytesPerRow$OFFSET`)
    }

    /**
     * Setter for field:
     * {@snippet lang=c :
     * * uint32_t bytesPerRow
     * * }
     */
    fun bytesPerRow(struct: MemorySegment, fieldValue: Int) {
        struct.set(`bytesPerRow$LAYOUT`, `bytesPerRow$OFFSET`, fieldValue)
    }

    private val `rowsPerImage$LAYOUT` = `$LAYOUT`.select(groupElement("rowsPerImage")) as ValueLayout.OfInt

    /**
     * Layout for field:
     * {@snippet lang=c :
     * * uint32_t rowsPerImage
     * * }
     */
    fun `rowsPerImage$layout`(): ValueLayout.OfInt {
        return `rowsPerImage$LAYOUT`
    }

    private const val `rowsPerImage$OFFSET`: Long = 20

    /**
     * Offset for field:
     * {@snippet lang=c :
     * * uint32_t rowsPerImage
     * * }
     */
    fun `rowsPerImage$offset`(): Long {
        return `rowsPerImage$OFFSET`
    }

    /**
     * Getter for field:
     * {@snippet lang=c :
     * * uint32_t rowsPerImage
     * * }
     */
    fun rowsPerImage(struct: MemorySegment): Int {
        return struct.get(`rowsPerImage$LAYOUT`, `rowsPerImage$OFFSET`)
    }

    /**
     * Setter for field:
     * {@snippet lang=c :
     * * uint32_t rowsPerImage
     * * }
     */
    fun rowsPerImage(struct: MemorySegment, fieldValue: Int) {
        struct.set(`rowsPerImage$LAYOUT`, `rowsPerImage$OFFSET`, fieldValue)
    }

    /**
     * Obtains a slice of `arrayParam` which selects the array element at `index`.
     * The returned segment has address `arrayParam.address() + index * layout().byteSize()`
     */
    fun asSlice(array: MemorySegment, index: Long): MemorySegment {
        return array.asSlice(layout().byteSize() * index)
    }

    /**
     * The size (in bytes) of this struct
     */
    fun sizeof(): Long {
        return layout().byteSize()
    }

    /**
     * Allocate a segment of size `layout().byteSize()` using `allocator`
     */
    fun allocate(allocator: SegmentAllocator): MemorySegment {
        return allocator.allocate(layout())
    }

    /**
     * Allocate an array of size `elementCount` using `allocator`.
     * The returned segment has size `elementCount * layout().byteSize()`.
     */
    fun allocateArray(elementCount: Long, allocator: SegmentAllocator): MemorySegment {
        return allocator.allocate(sequenceLayout(elementCount, layout()))
    }

    /**
     * Reinterprets `addr` using target `arena` and `cleanupAction` (if any).
     * The returned segment has size `layout().byteSize()`
     */
    fun reinterpret(addr: MemorySegment, arena: Arena, cleanup: Consumer<MemorySegment?>): MemorySegment {
        return reinterpret(addr, 1, arena, cleanup)
    }

    /**
     * Reinterprets `addr` using target `arena` and `cleanupAction` (if any).
     * The returned segment has size `elementCount * layout().byteSize()`
     */
    fun reinterpret(
        addr: MemorySegment,
        elementCount: Long,
        arena: Arena,
        cleanup: Consumer<MemorySegment?>
    ): MemorySegment {
        return addr.reinterpret(layout().byteSize() * elementCount, arena, cleanup)
    }
}

