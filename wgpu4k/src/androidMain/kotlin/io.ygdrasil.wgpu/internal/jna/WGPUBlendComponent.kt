// Generated by jextract
package io.ygdrasil.wgpu.internal.jna

import java.lang.foreign.Arena
import java.lang.foreign.GroupLayout
import java.lang.foreign.MemoryLayout.Companion.sequenceLayout
import java.lang.foreign.MemoryLayout.Companion.structLayout
import java.lang.foreign.MemoryLayout.PathElement.groupElement
import java.lang.foreign.MemorySegment
import java.lang.foreign.SegmentAllocator
import java.lang.foreign.ValueLayout
import java.util.function.Consumer

/**
 * {@snippet lang=c :
 * * struct WGPUBlendComponent {
 * *     WGPUBlendOperation operation;
 * *     WGPUBlendFactor srcFactor;
 * *     WGPUBlendFactor dstFactor;
 * * }
 * * }
 */
object WGPUBlendComponent {
    private val `$LAYOUT` = structLayout(
        wgpu_h.C_INT.withName("operation"),
        wgpu_h.C_INT.withName("srcFactor"),
        wgpu_h.C_INT.withName("dstFactor")
    ).withName("WGPUBlendComponent")

    /**
     * The layout of this struct
     */
    @JvmStatic
    fun layout(): GroupLayout {
        return `$LAYOUT`
    }

    private val `operation$LAYOUT` = `$LAYOUT`.select(groupElement("operation")) as ValueLayout.OfInt

    /**
     * Layout for field:
     * {@snippet lang=c :
     * * WGPUBlendOperation operation
     * * }
     */
    fun `operation$layout`(): ValueLayout.OfInt {
        return `operation$LAYOUT`
    }

    private const val `operation$OFFSET`: Long = 0

    /**
     * Offset for field:
     * {@snippet lang=c :
     * * WGPUBlendOperation operation
     * * }
     */
    fun `operation$offset`(): Long {
        return `operation$OFFSET`
    }

    /**
     * Getter for field:
     * {@snippet lang=c :
     * * WGPUBlendOperation operation
     * * }
     */
    fun operation(struct: MemorySegment): Int {
        return struct.get(`operation$LAYOUT`, `operation$OFFSET`)
    }

    /**
     * Setter for field:
     * {@snippet lang=c :
     * * WGPUBlendOperation operation
     * * }
     */
    fun operation(struct: MemorySegment, fieldValue: Int) {
        struct.set(`operation$LAYOUT`, `operation$OFFSET`, fieldValue)
    }

    private val `srcFactor$LAYOUT` = `$LAYOUT`.select(groupElement("srcFactor")) as ValueLayout.OfInt

    /**
     * Layout for field:
     * {@snippet lang=c :
     * * WGPUBlendFactor srcFactor
     * * }
     */
    fun `srcFactor$layout`(): ValueLayout.OfInt {
        return `srcFactor$LAYOUT`
    }

    private const val `srcFactor$OFFSET`: Long = 4

    /**
     * Offset for field:
     * {@snippet lang=c :
     * * WGPUBlendFactor srcFactor
     * * }
     */
    fun `srcFactor$offset`(): Long {
        return `srcFactor$OFFSET`
    }

    /**
     * Getter for field:
     * {@snippet lang=c :
     * * WGPUBlendFactor srcFactor
     * * }
     */
    fun srcFactor(struct: MemorySegment): Int {
        return struct.get(`srcFactor$LAYOUT`, `srcFactor$OFFSET`)
    }

    /**
     * Setter for field:
     * {@snippet lang=c :
     * * WGPUBlendFactor srcFactor
     * * }
     */
    fun srcFactor(struct: MemorySegment, fieldValue: Int) {
        struct.set(`srcFactor$LAYOUT`, `srcFactor$OFFSET`, fieldValue)
    }

    private val `dstFactor$LAYOUT` = `$LAYOUT`.select(groupElement("dstFactor")) as ValueLayout.OfInt

    /**
     * Layout for field:
     * {@snippet lang=c :
     * * WGPUBlendFactor dstFactor
     * * }
     */
    fun `dstFactor$layout`(): ValueLayout.OfInt {
        return `dstFactor$LAYOUT`
    }

    private const val `dstFactor$OFFSET`: Long = 8

    /**
     * Offset for field:
     * {@snippet lang=c :
     * * WGPUBlendFactor dstFactor
     * * }
     */
    fun `dstFactor$offset`(): Long {
        return `dstFactor$OFFSET`
    }

    /**
     * Getter for field:
     * {@snippet lang=c :
     * * WGPUBlendFactor dstFactor
     * * }
     */
    fun dstFactor(struct: MemorySegment): Int {
        return struct.get(`dstFactor$LAYOUT`, `dstFactor$OFFSET`)
    }

    /**
     * Setter for field:
     * {@snippet lang=c :
     * * WGPUBlendFactor dstFactor
     * * }
     */
    fun dstFactor(struct: MemorySegment, fieldValue: Int) {
        struct.set(`dstFactor$LAYOUT`, `dstFactor$OFFSET`, fieldValue)
    }

    /**
     * Obtains a slice of `arrayParam` which selects the array element at `index`.
     * The returned segment has address `arrayParam.address() + index * layout().byteSize()`
     */
    fun asSlice(array: MemorySegment, index: Long): MemorySegment {
        return array.asSlice(layout().byteSize() * index)
    }

    /**
     * The size (in bytes) of this struct
     */
    fun sizeof(): Long {
        return layout().byteSize()
    }

    /**
     * Allocate a segment of size `layout().byteSize()` using `allocator`
     */
    fun allocate(allocator: SegmentAllocator): MemorySegment {
        return allocator.allocate(layout())
    }

    /**
     * Allocate an array of size `elementCount` using `allocator`.
     * The returned segment has size `elementCount * layout().byteSize()`.
     */
    fun allocateArray(elementCount: Long, allocator: SegmentAllocator): MemorySegment {
        return allocator.allocate(sequenceLayout(elementCount, layout()))
    }

    /**
     * Reinterprets `addr` using target `arena` and `cleanupAction` (if any).
     * The returned segment has size `layout().byteSize()`
     */
    fun reinterpret(addr: MemorySegment, arena: Arena, cleanup: Consumer<MemorySegment?>): MemorySegment {
        return reinterpret(addr, 1, arena, cleanup)
    }

    /**
     * Reinterprets `addr` using target `arena` and `cleanupAction` (if any).
     * The returned segment has size `elementCount * layout().byteSize()`
     */
    fun reinterpret(
        addr: MemorySegment,
        elementCount: Long,
        arena: Arena,
        cleanup: Consumer<MemorySegment?>
    ): MemorySegment {
        return addr.reinterpret(layout().byteSize() * elementCount, arena, cleanup)
    }
}

